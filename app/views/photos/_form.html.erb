<%= form_for([@folder, @folder.photos.build]) do |f| %>
  <div class="field">
	<h3>Add new photo:</h3>
  	<%= f.file_field :avatar %>
</div>
<div class="field"> 
 	<%= f.label :tag_list %><br>
 	<%= f.text_field :tag_list %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
